set key1 0(是否启用压缩) 0(有效期,默认30天,单位为秒) 10(长度) 
set   键名　 是否启用压缩(0不启用/1启用)   缓存时间(默认为0表示最大有效期为30天)  　字符长度
memcache的cache机制是怎样的?
1、默认情况下使用slab进行内存分配
2、使用LRU(最近最少使用) + 超时失效算法
memcache如何实现冗余机制?
没有实现冗余机制
memcache如何处理容错的?
没有进行容错
如何将memcache中item批量导入导出?
不建议进行导入导出数据，因为在导入导出时，可能会有数据失效，会产生一些脏数据(本应该失效的数据)。